---
# defaults file for dev-tools

vim:
  name: "vim"
  prefix: "{{ ansible_env.HOME }}/.vim"
  init: "{{ ansible_env.HOME }}/.vimrc"

neovim:
  name: "neovim"
  prefix: "{{ ansible_env.HOME }}/.config/nvim"
  init: "{{ ansible_env.HOME }}/.config/nvim/init.vim"
